function Home() {
  return (
    <div>
      <h1>Home</h1>
      {/* <Carousel>
        <Carousel.Body contentSize={crewData.length}>
          <Carousel.Content
            data={crewData}
            render={(data, key) => <div key={key}>{(data as Crew).bio}</div>}
          />
        </Carousel.Body>
      </Carousel> */}
    </div>
  );
}

export default Home;
