export default class AppImageAssets {
    static defUrl = "/assets/";
    static crewDefUrl = `${this.defUrl}/`;
    static sharedDefUrl = `${this.defUrl}/shared`;

    static logo = `${this.sharedDefUrl}/logo.svg`;
    static crewBackgroundMobile = `${this.crewDefUrl}/`;
    static crewBackgroundTablet = `${this.crewDefUrl}/`;
    static crewBackgroundDesktop = `${this.crewDefUrl}/`;

}